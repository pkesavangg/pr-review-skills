# macOS
.DS_Store

# VS Code
*.code-workspace

# Xcode
*.xcworkspace
*.xcuserstate
*.xcuserdata
*.xcodeproj/project.xcworkspace/
*.xcodeproj/xcuserdata/
.derived-data-log-*
DerivedData/
.build/

# Android
*.iml
.gradle/
build/
local.properties
.idea/
*.log

# General
*.swp
*.swo
*.bak
*.tmp

# Sensitive files - Q1 2026 audit
.env
.env.*
*.pem
*.key
credentials.json
secrets.json
GoogleService-Info.plist
**/GoogleService-Info.plist

# Refact.ai extension state
.refact/

# Claude skill transient state
Android/.claude/*.json
# Lefthook local overrides (developer-specific)
lefthook-local.yml

# Developer-local Claude Code settings (settings.local.json, cache, transcripts) are
# ignored by default; shared, team-committed AI-context is re-included below.
# NOTE: use `.claude/*` (not `.claude/`) so git descends and the `!` re-includes apply,
# and list each subtree explicitly — a bare `!.claude/skills/` does NOT cover iOS/Android.
.claude/*
iOS/.claude/*
Android/.claude/*
!.claude/skills/
!.claude/settings.json
!iOS/.claude/skills/
!iOS/.claude/agents/
!iOS/.claude/commands/
!iOS/.claude/orchestra.md
!iOS/.claude/MCP_SERVERS.md
!iOS/.claude/settings.json
!Android/.claude/skills/

# Developer convenience scripts
Android/run.sh
.refact/
