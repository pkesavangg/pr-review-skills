//
//  AccessibilityID+Dashboard.swift
//  Dashboard feature — weight / blood-pressure / baby cards.
//

extension AccessibilityID {
    // MARK: - Dashboard
    static let weightCard = "weight_card"
    static let bpCard = "bp_card"
    static let babyCard = "baby_card"

    // MARK: - Bottom tab bar (suffixed with the tab's raw value, e.g. _dash)
    static let tabBarItem = "tab_bar_item"
}
