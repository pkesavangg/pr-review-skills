//
//  AccessibilityID+Feed.swift
//  Feed feature — social/activity feed.
//
//  Placeholder home for Feed ids. The Feed feature currently ships only
//  Constants + Domain models (`Features/Feed/` has no SwiftUI list/screen yet); the
//  user-facing message feed is served by the Messages/IAM screen. Add `feed_*` ids
//  here (e.g. `feed_list`, `feed_item`, `feed_item_action`) when the Feed list UI lands.
//

extension AccessibilityID {
    // MARK: - Feed (no dedicated Feed list view in-app yet)
}
