APP_ENV = DEV
// Scheme (http://) is prepended in Environment.swift — xcconfig treats // as a comment,
// so the base URL here must be scheme-less.
API_BASE_URL = ec2-13-217-141-203.compute-1.amazonaws.com:3005/v3
