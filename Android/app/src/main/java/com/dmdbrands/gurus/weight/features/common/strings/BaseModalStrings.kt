package com.dmdbrands.gurus.weight.features.common.strings

/**
 * Static strings for BaseModal composable.
 */
object BaseModalStrings {
    // No static text in BaseModal itself, as all text is passed as parameters.
    // Add here if any static text is introduced in the future.
}
