package com.dmdbrands.gurus.weight.features.common.strings

/**
 * Static strings for AppInput composable.
 */
object AppInputStrings {
    const val EmptySpace = " "

    // region Accessibility (TalkBack)
    /** Trailing-icon label when the password is hidden (tapping reveals it). */
    const val accShowPasswordLabel = "Show password"

    /** Trailing-icon label when the password is visible (tapping hides it). */
    const val accHidePasswordLabel = "Hide password"

    /** Trailing-icon label for the clear-text button. */
    const val accClearLabel = "Clear"
    // endregion
}
